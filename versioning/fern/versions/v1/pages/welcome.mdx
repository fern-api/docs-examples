---
title: Welcome
subtitle: You're viewing the v1 (Legacy) version of these docs
slug: welcome
---

<Warning>
  v1 is a legacy version kept online for reference only. It is no longer receiving updates. See the [upgrade notice](/v1/upgrade-notice) for the recommended migration path.
</Warning>

Because this version has `slug: v1` set in `docs.yml`, its pages are served from `/v1/*` URLs — for example, `/v1/welcome` and `/v1/upgrade-notice`.
